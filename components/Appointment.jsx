import React from 'react';import styled from "styled-components/native";import {View} from "react-native";import {SectionTitle} from "./SectionTitle";import GrayText from "./GreyTxt"import Badge from "../screen/Badge"export const Appointment = ({navigate,item}) => {    const {user, diagnosis, active, time, title,} = item    return (    <Groups>        <SectionTitle>{title}</SectionTitle>            <GroupItem onPress={navigate.bind(this,'Patient',item)}>                <Avatar                    source={{url: user.avatar}}/>                <View style={{flex: 1}}>                    <FullName>{user.fullName}</FullName>                    <GrayText>{diagnosis}</GrayText>                </View>                <Badge active={active} short={true}>{time}</Badge>            </GroupItem>        </Groups>    )}Appointment.defoultProps = {    groupTitle: 'Untitled',    items: []}const Avatar = styled.Image`            border-radius:50px            width:40px;            height:40px;            margin-right:15px;            `;const FullName = styled.Text`            font-weight:600;            font-size:16px;            `;const GroupItem = styled.TouchableOpacity`            padding:3px 20px;            flex-direction:row;            align-items:center;            border-bottom-color:#f3f3f3;            `;const Groups = styled.TouchableOpacity`         padding: 0 10px;                             `;